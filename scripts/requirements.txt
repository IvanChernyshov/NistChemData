# Core dependencies for NistChemData local reconstruction scripts.
#
# The scripts currently target NistChemPy 1.0.6 as the WebBook access/index
# layer because this version still exposes the packaged WebBook index used by
# scripts/common.py. Do not relax this pin until NistChemPy's planned local-index
# refactor is reflected in NistChemData.
nistchempy==1.0.6

# Shared script/runtime dependencies.
tqdm
pandas

# Required only by process_ir_spectra.py for reading JCAMP-DX/JDX headers.
jcamp
